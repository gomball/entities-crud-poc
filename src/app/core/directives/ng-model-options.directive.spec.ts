import { NgModelOptionsDirective } from './ng-model-options.directive';

describe('NgModelOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelOptionsDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
