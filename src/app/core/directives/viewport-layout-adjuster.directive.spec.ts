import { ViewportLayoutAdjusterDirective } from './viewport-layout-adjuster.directive';

describe('ViewportLayoutAdjusterDirective', () => {
  it('should create an instance', () => {
    // const directive = new LayoutAdjusterDirective();
    // expect(directive).toBeTruthy();
  });
});
