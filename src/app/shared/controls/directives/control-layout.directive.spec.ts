import { ControlLayoutDirective } from './control-layout.directive';

describe('ControlLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlLayoutDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
